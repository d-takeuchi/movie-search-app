import React from 'react';

const SearchInput = () => {
  return (
    <div>
      <input type="text" placeholder="映画タイトルを入力してください" />
      <button>検索ボタン</button>
    </div>
  );
};

export default SearchInput;
